# MongoDB Atlas connection string (use SRV format or standard)
# Ejemplo: mongodb+srv://<user>:<password>@cluster0.mongodb.net/<dbname>?retryWrites=true&w=majority
MONGO_URI=

# Email (Gmail recommended with App Passwords)
EMAIL_USER=
EMAIL_PASS=

# URL del frontend para CORS (opcional). Ej: https://midominio.com
FRONTEND_URL=

# Optional: puerto (Render asignará su propio PORT automáticamente)
PORT=3000
